import { notesStore } from './src/lib/stores/notes.svelte.ts';
import { folderStore } from './src/lib/stores/folders.svelte.ts';

// this is a browser component system so we can't easily mock it outside Svelte 5 without a heavy test wrapper
